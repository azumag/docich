/* Gutted: GNU Guile 3.x provides `random' and friends natively, and the
   1.x-era gh_* API this file was written against no longer exists.  Kept
   as an empty translation unit so the original Makefile objects list
   stays valid. */
#include <libguile.h>

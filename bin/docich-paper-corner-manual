#!/usr/bin/env bash
# 手動PAPERコーナー試験ランナー（当日22:00枠とは別state/lock）。
set -euo pipefail
ROOT="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")/.." >/dev/null 2>&1 && pwd)"
export PYTHONPATH="$ROOT/src${PYTHONPATH:+:$PYTHONPATH}"
exec python3 -m docich.paper_corner_manual "$@"

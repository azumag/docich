name: TDD prediction worker status red

on:
  push:
    branches:
      - codex/prediction-worker-status-reasons

jobs:
  red:
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    steps:
      - name: Runner probe
        run: echo runner-ok

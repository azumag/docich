[game]
name = "sorengame"
title = "soren game (Unity WebGL)"
adapter = "browser"

[browser]
url = "http://127.0.0.1:8080"  # soren が同一VMで公開するローカルWebGL viewer
kiosk = true
binary = "auto"

[agent]
enabled = false  # viewer専用。現在の本番運転はsoviet_nowが所有する
